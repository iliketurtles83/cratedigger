# Required
mutagen>=1.47
python-dotenv>=1.0

# Optional — fingerprinting & MusicBrainz
# NOTE: pyacoustid also requires the fpcalc binary:
# sudo apt install libchromaprint-tools
pyacoustid>=1.3
musicbrainzngs>=0.7

# Optional — BPM detection
librosa>=0.10

# Phase 5 — audio analysis (06_analyze.py)
pandas>=2.0
pyloudnorm>=0.1
